# Daily — 2026-04-20

## Session Learnings

- [webapp] `vite-plugin-fontaine` n'existe pas — le bon package est `fontaine` (expose `FontaineTransform.vite()`). Plan mentionnait le mauvais nom. Discovered in sablia-site-ux-migration phase 1.
- [webapp] `@fontsource-variable/fraunces` root import (`index.css`) ne charge QUE l'axe `wght`. Pour tous les axes (SOFT, WONK, opsz + wght) il faut importer `/full.css` + `/full-italic.css`. Discovered in sablia-site-ux-migration phase 1.
- [webapp] Shim strategy pour migrations de tokens Tailwind : redéfinir les anciens noms de tokens avec les NOUVELLES valeurs hex (pas CSS vars) — slash opacity fonctionne nativement v3.4, et Phase de propagation peut supprimer les shims sans toucher aux valeurs. Discovered in sablia-site-ux-migration phase 1.
